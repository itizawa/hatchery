import { test } from "../support/test.js";

/**
 * ranking e2e テスト（#665）。
 *
 * e2e/ranking/usecases.md の UC-RANK-01〜03 に対応するテストスケルトン。
 */

test.todo("UC-RANK-01: サイドバーの「ランキング」リンクを押して /ranking へ遷移できる");

test.todo(
  "UC-RANK-02: ランキング画面にワーカーの表示名・閲覧数（7日）・Vote スコア（7日）と順位番号が表示される",
);

test.todo(
  "UC-RANK-03: 直近 7 日以内に閲覧も vote も記録されていない場合に空状態メッセージが表示される",
);
