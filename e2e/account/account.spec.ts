import { test } from "../support/test.js";

/**
 * account（アカウント設定 / プロフィール編集）の e2e スケルトン（Issue #472）。
 * 同ディレクトリの usecases.md のユースケース見出しと 1:1 で対応する test.todo()。
 * 実テストへの置き換えは個別 Issue で順次対応する。
 */

test.todo("UC-ACCOUNT-01: 表示名・プロフィール画像 URL を変更して保存できる");

test.todo("UC-ACCOUNT-02: 変更が無いとき保存ボタンが無効化される");

test.todo("UC-ACCOUNT-03: 不正な URL を入力すると保存できずエラーが表示される");

test.todo("UC-ACCOUNT-04: プロフィール更新に失敗するとエラー内容が表示される");
</content>
