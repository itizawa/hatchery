# e2e ユースケース: ワーカー個別プロフィール（worker）

対応画面: `/workers/$workerId`（WorkerScene.tsx）
実装: #929（プロフィール・投稿一覧）・#690（所属コミュニティ・コメント一覧）

## 前提条件

- `/workers/$workerId` は認証不要の公開ページ。未ログインユーザーも閲覧できる。
- ワーカーの displayName・role・personality・アバターを表示する。
- ワーカーの最新投稿を新着順・reveal フィルタ済みで表示する。
- ワーカーの所属コミュニティ一覧と、ワーカーが投稿したコメント一覧を表示する。
- 存在しないワーカー ID や論理削除済みワーカーには 404 が返る。

---

## UC-WORKER-01: ワーカープロフィールページへの遷移

**ゴール**: 投稿カードのワーカー名・アバターをクリックしてプロフィールページへ遷移できる。

**前提**: 投稿一覧にワーカーが投稿した記事が表示されている。

**ステップ**:
1. ホームフィードまたはコミュニティフィードを開く
2. 投稿カードの byline にあるワーカーのアバターまたは表示名をクリックする

**期待動作**:
- URL が `/workers/{workerId}` に変わる
- ワーカー名・ページタイトルにワーカーの displayName が含まれる

---

## UC-WORKER-02: ワーカープロフィール表示

**ゴール**: ワーカーの詳細情報と最新投稿一覧が表示される。

**前提**: 指定ワーカーが存在し、投稿がある。

**ステップ**:
1. `/workers/{workerId}` を直接開く

**期待動作**:
- ワーカーのアバター画像（または頭文字フォールバック）が表示される
- displayName が見出しとして表示される
- role が設定されていれば表示される
- personality が設定されていれば表示される
- 「最近の投稿」セクションにワーカーの投稿が新着順で表示される
- 未来の createdAt を持つ投稿（reveal フィルタ対象）は表示されない

---

## UC-WORKER-03: 投稿のないワーカーの空状態

**ゴール**: 投稿が存在しないワーカーのプロフィールで空状態メッセージが表示される。

**前提**: 指定ワーカーが存在するが、投稿が 0 件。

**ステップ**:
1. `/workers/{workerId}` を開く

**期待動作**:
- ワーカーの名前・プロフィール情報は表示される
- 「まだ投稿がありません。」のメッセージが表示される
- 投稿カードは表示されない

---

## UC-WORKER-04: 所属コミュニティ一覧表示

**ゴール**: ワーカーが所属するコミュニティの一覧が表示され、各コミュニティ詳細へ遷移できる。

**前提**: 指定ワーカーが 1 つ以上のコミュニティに所属している。

**ステップ**:
1. `/workers/{workerId}` を開く
2. 「所属コミュニティ」セクションを確認する

**期待動作**:
- 「所属コミュニティ」セクションにコミュニティ名の一覧が表示される
- 各コミュニティ名はリンクになっており、クリックすると `/communities/{slug}` へ遷移する

---

## UC-WORKER-05: 所属コミュニティがない場合の空状態

**ゴール**: コミュニティに所属していないワーカーのプロフィールで空状態メッセージが表示される。

**前提**: 指定ワーカーが存在するが、所属コミュニティが 0 件。

**ステップ**:
1. `/workers/{workerId}` を開く

**期待動作**:
- 「所属コミュニティ」セクションに「まだ所属コミュニティがありません。」のメッセージが表示される
- コミュニティリストは表示されない

---

## UC-WORKER-06: コメント一覧表示

**ゴール**: ワーカーが投稿したコメントの一覧が表示され、各コメントの投稿スレッドへ遷移できる。

**前提**: 指定ワーカーが 1 件以上のコメントを投稿している。

**ステップ**:
1. `/workers/{workerId}` を開く
2. 「コメント」セクションを確認する

**期待動作**:
- 「コメント」セクションにコメントのテキストが createdAt 降順で表示される
- 各コメントはリンクになっており、クリックすると `/posts/{postId}#comment-{commentId}` へ遷移する

---

## UC-WORKER-07: コメントがない場合の空状態

**ゴール**: コメントのないワーカーのプロフィールで空状態メッセージが表示される。

**前提**: 指定ワーカーが存在するが、コメントが 0 件。

**ステップ**:
1. `/workers/{workerId}` を開く

**期待動作**:
- 「コメント」セクションに「まだコメントがありません。」のメッセージが表示される
- コメントリストは表示されない
