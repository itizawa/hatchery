import { test } from "../support/test.js";

/**
 * dashboard e2e テスト（#1113）。
 *
 * e2e/dashboard/usecases.md の UC-DASHBOARD-01〜03 に対応するテスト。
 * 実装は別途行う（現時点は todo として登録し、usecases.md との 1:1 対応を保つ）。
 */

test.todo("UC-DASHBOARD-01: 未ログインでもダッシュボードにアクセスしてサマリとコミュニティ別内訳が表示される");

test.todo("UC-DASHBOARD-02: サイドバーの「ダッシュボード」リンクを押して /dashboard へ遷移できる");

test.todo("UC-DASHBOARD-03: コミュニティが存在しない場合の空状態表示");
