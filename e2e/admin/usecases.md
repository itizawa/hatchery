# admin — 管理画面（Worker / Community 管理）のユースケース

対応画面: `client/src/routes/SettingsScene.tsx`（/admin、タブ構成）・対応 API: `client/src/api/admin.ts`

/admin は admin ロール専用（未ログインは `/?login=1`、非 admin は `/` へリダイレクト・#136）。
各ユースケースの見出し（`## UC-ADMIN-NN: ...`）は `admin.spec.ts` の `test()` と 1:1 で対応する。

## UC-ADMIN-01: 未ログインで /admin にアクセスすると /?login=1 へリダイレクトされログインモーダルが開く

- 前提条件: 未ログイン状態。
- ステップ:
  1. `/admin` を直接開く。
- 期待動作: `/?login=1` へリダイレクトされ、ホーム上にログインモーダルが開く。管理画面の内容は表示されない。
- 備考: #454 で `/login` ルートは廃止され `/?login=1` モーダル方式へ移行済み。`router.tsx` の `requireAdminRoute` が認証失敗時に `redirect({ to: "/", search: { login: true } })` を投げる。

## UC-ADMIN-02: 非 admin ユーザーが /admin にアクセスするとホームへリダイレクトされる

- 前提条件: 一般（非 admin）ユーザーでログイン済み。
- ステップ:
  1. `/admin` を直接開く。
- 期待動作: `/`（ホーム）へリダイレクトされ、管理画面の内容は表示されない。

## UC-ADMIN-03: admin ユーザーは管理画面のタブを切り替えられる

- 前提条件: admin ユーザーでログイン済み。
- ステップ:
  1. `/admin` を開く。
  2. 各タブ（ユーザー・Worker・コミュニティ等）を順にクリックする。
- 期待動作: タブごとの管理 UI が表示され、URL の `?tab=` が切り替わる。

## UC-ADMIN-04: admin ユーザーが Worker 一覧を閲覧できる

- 前提条件: admin ユーザーでログイン済み。Worker が 1 件以上登録されている。
- ステップ:
  1. `/admin` を開き Worker タブへ移動する。
- 期待動作: 登録済み Worker の一覧（名前・ペルソナ等）がテーブル表示される。

## UC-ADMIN-05: admin ユーザーが Worker を新規作成できる

- 前提条件: admin ユーザーでログイン済み。
- ステップ:
  1. `/admin` の Worker タブで「ワーカーを追加」ボタンをクリックする。
  2. `/admin/workers/new`（Worker 作成専用ページ）へ遷移する（#888）。
  3. 作成フォームに必須項目（表示名）を入力する。
  4. 「追加」ボタンをクリックして作成を実行する。
- 期待動作: 作成成功後に `/admin/workers/$workerId/edit`（編集ページ）へ遷移し、再読込後も Worker が存在する。

## UC-ADMIN-06: admin ユーザーが Worker を削除できる

- 前提条件: admin ユーザーでログイン済み。削除可能な Worker が存在する。
- ステップ:
  1. `/admin` の Worker タブで対象 Worker の削除を実行する。
- 期待動作: 一覧から対象 Worker が消え、再読込後も表示されない。
- **現状のギャップ（#430）**: 削除 API（`DELETE /api/admin/workers/:id`）は実装済みだが、
  `AdminWorkerTableInner` が `WorkerTable` へ `onDelete` prop を渡していないため、
  削除ボタンが管理画面 UI に表示されない。現テストは「削除ボタンが表示されていない」現状を検証する。
  削除 UI の接続は別 Issue で対応する。

## UC-ADMIN-07: admin ユーザーがコミュニティ管理タブで一覧を閲覧できる

- 前提条件: admin ユーザーでログイン済み。コミュニティが 1 件以上存在する。
- ステップ:
  1. `/admin` のコミュニティタブへ移動する。
- 期待動作: コミュニティの一覧（名前・slug 等）が管理 UI に表示される。

## UC-ADMIN-08: admin ユーザーが Worker の参加コミュニティを編集できる

- 前提条件: admin ユーザーでログイン済み。コミュニティが 1 件以上・Worker が 1 件以上存在する。
- ステップ:
  1. `/admin` の Worker タブで対象 Worker の「編集」ボタンをクリックし、`/admin/workers/$workerId/edit`（編集専用ページ）へ遷移する（#888）。
  2. 「参加コミュニティ」の複数選択で 1 つ以上のコミュニティを選び、保存する。
- 期待動作: 保存が成功し、再度同じ Worker の編集ページを開くと、選択した参加コミュニティが選択状態で表示される（紐づきが永続化されている）。

## UC-ADMIN-09: admin ユーザーが Worker 新規作成時に参加コミュニティを指定できる

- 前提条件: admin ユーザーでログイン済み。コミュニティが 1 件以上存在する。
- ステップ:
  1. `/admin/workers/new`（Worker 作成専用ページ）で必須項目（表示名）を入力する（#888）。
  2. 「参加コミュニティ」の複数選択でコミュニティを選び、作成を実行する。
- 期待動作: Worker が作成され、`/admin/workers/$workerId/edit` の編集ページで、作成時に指定した参加コミュニティが選択状態で表示される。

## UC-ADMIN-10: admin ユーザーがコミュニティのアイコン・カバー画像をアップロードできる

- 前提条件: admin ユーザーでログイン済み。コミュニティが 1 件以上存在する。
- ステップ:
  1. `/admin` のコミュニティタブで対象コミュニティの「編集」ボタンを押し、編集モーダルダイアログを開く（#833）。
  2. ダイアログ内のカバー画像のアップロード領域、またはアイコン画像のアバターをクリックし、画像ファイル（png/jpeg/webp/gif・5MB 以下）を選ぶ。
- 期待動作:
  - アップロードが成功し、編集ダイアログ内のプレビュー（アイコン / カバー）に選んだ画像が反映される（ダイアログは開いたまま）。
  - 該当コミュニティの詳細ページ（`/communities/$slug`）を開くと、Reddit 風ヘッダーにアップロードした画像が表示される。

## UC-ADMIN-11: 管理画面タブのデータ取得に失敗すると再試行フォールバックが表示される

- 前提条件: admin ユーザーでログイン済み。各タブ（Worker 一覧・バッチログ・トークン使用量等）のデータ取得 API が一時的に失敗する状況。
- ステップ:
  1. `/admin` を開く（または該当タブへ移動する）。
  2. 当該タブのデータ取得が失敗する。
- 期待動作: 取得待ちの間はそのタブ領域にローディング（スケルトン）が表示され、取得に失敗した場合は「データの取得に失敗しました。」と「再試行」ボタンを含むフォールバックがそのタブ領域内に表示される。「再試行」を押すと再取得され、成功すれば本来のデータ表示に戻る。管理画面の見出し・タブ自体は失敗しても表示され続ける（失敗の影響はタブ領域内に閉じる）。

## UC-ADMIN-12: Worker 編集の保存に失敗するとエラー内容が表示される

- 前提条件: admin ユーザーでログイン済み。編集対象の Worker が存在する。
- ステップ:
  1. `/admin` の Worker タブで対象 Worker の編集を開く。
  2. 何らかの理由で保存が失敗する状況（権限・サーバエラー・ネットワーク断等）で保存を実行する。
- 期待動作: 「失敗した」だけでなく、サーバが返した具体的なエラー内容（種別レベル以上の文言）がエラー表示（Snackbar）に出る。エラー表示を閉じて再度ダイアログを開き直しても、前回のエラーが残留・誤表示されない。

## UC-ADMIN-15: admin ユーザーが Worker の文章量（verbosity）を編集できる（#625）

- 前提条件: admin ユーザーでログイン済み。Worker が 1 件以上存在する。
- ステップ:
  1. `/admin` の Worker タブで対象 Worker の編集を開く。
  2. 「文章量」の選択 UI（簡潔 / 標準 / 詳細）から任意の値を選ぶ。
  3. 保存を実行する。
- 期待動作:
  - 保存が成功し、再度同じ Worker の編集を開くと、変更した文章量が選択状態で表示される（設定が永続化されている）。
  - 既存の Worker（verbosity 未設定）は「標準」が初期選択になる。

## UC-ADMIN-16: 管理画面の Worker 一覧がページネーション（10件/ページ）で表示される (#545)

- 前提条件: admin ユーザーでログイン済み。Worker が 11 件以上登録されている。
- ステップ:
  1. `/admin?tab=workers`（Worker タブ）を開く。
  2. 一覧に 10 件表示されていることを確認する。
  3. ページネーション UI（TablePagination）で「次のページ」をクリックする。
- 期待動作:
  - 初期表示で最初の 10 件が表示され、総件数がページネーション領域に表示される。
  - 「次のページ」クリック後、次の 10 件に切り替わり、ページ番号表示が更新される。
  - 最初のページに戻る「前のページ」操作で元の 10 件に戻る。

## UC-ADMIN-14: admin ユーザーがコミュニティに生成プロンプト指示（非公開）を設定できる

- 前提条件: admin ユーザーでログイン済み。コミュニティが 1 件以上存在する。
- ステップ:
  1. `/admin?tab=communities` を開く。
  2. 「コミュニティを追加」ボタンを押して作成モーダルダイアログを開く（#833）。
  3. ダイアログでスラグ・名前・概要（公開）を入力し、「生成プロンプト指示（管理者のみ・非公開）」欄に指示文（例：「脱さん付け・率直に話す。」）を入力する。
  4. 「作成」を実行する。
  5. 一覧に新しいコミュニティが表示されたあと「編集」をクリックして編集モーダルダイアログを開き、生成プロンプト指示欄に値が入っていることを確認する。
  6. 指示文を書き換えて「保存」を実行する。
- 期待動作:
  - 作成後、成功スナックバーが表示される。作成・編集の操作はモーダルダイアログで行われる（#833）。
  - 一般ユーザー向け公開 API（`GET /api/communities`）のレスポンスに `generationInstruction` が含まれない（非公開が担保されている）。
  - 定時バッチ実行後の投稿・コメントの雰囲気が、設定した指示の内容を反映する。

## UC-ADMIN-17: 管理画面のトークン使用量タブにコスト（$）が表示される（#664）

- 前提条件: admin ユーザーでログイン済み。定時バッチが少なくとも 1 回実行済みでトークン使用量ログが存在する。
- ステップ:
  1. `/admin?tab=token-usage` を開く。
- 期待動作:
  - 「合計」サマリボックスに「推定コスト: $X.XXXXXX」形式でコストが表示される。
  - 表示コストは 0 以上（トークン使用量がある場合は正の値）。

## UC-ADMIN-18: 管理画面のトークン使用量タブに日別コスト推移グラフが表示される（#664）

- 前提条件: admin ユーザーでログイン済み。トークン使用量ログが 1 件以上存在する。
- ステップ:
  1. `/admin?tab=token-usage` を開く。
- 期待動作:
  - 「日別コスト推移」見出しの下にバーチャートが表示される。
  - バーはログの日付単位で集計され、コストが高い日ほど高いバーで描画される。
  - 各バーにカーソルを当てると日付と金額のツールチップが表示される。

## UC-ADMIN-19: admin ユーザーが帰属シグナルタブでコミュニティ帰属状況を閲覧できる（#761）

- 前提条件: admin ユーザーでログイン済み。
- ステップ:
  1. `/admin?tab=community-engagement` を開く（または管理画面から「帰属シグナル」タブをクリックする）。
- 期待動作:
  - 「直近 30 日間のコミュニティ帰属シグナル」説明文が表示される。
  - ロイヤリティスコア（0〜100%）が数値で表示される。
  - コミュニティ別 Vote 分布テーブル（コミュニティ ID・Vote 数・シェア）が表示される（データがない場合は「データがありません。」と表示される）。
  - ワーカー別 Vote 分布テーブル（ワーカー ID・Vote 数・シェア）が表示される（データがない場合は「データがありません。」と表示される）。
  - コミュニティ別購読者数テーブル（コミュニティ ID・購読者数）が表示される（データがない場合は「データがありません。」と表示される）。

## UC-ADMIN-20: admin ユーザーが Worker 作成専用ページ（/admin/workers/new）でワーカーを作成できる（#888）

- 前提条件: admin ユーザーでログイン済み。
- ステップ:
  1. `/admin/workers/new` を直接開く、または `/admin?tab=workers` の「ワーカーを追加」ボタンをクリックして遷移する。
  2. 「表示名」フィールドに名前を入力する。
  3. 任意で「役割」・「参加コミュニティ」を入力・選択する。
  4. 「追加」ボタンをクリックする。
- 期待動作:
  - 「ワーカーを追加」という見出しが表示される。
  - 「← 一覧に戻る」リンクで `/admin?tab=workers` へ戻れる。
  - 表示名が空のとき「追加」ボタンが無効化されている。
  - 作成成功後、`/admin/workers/$workerId/edit`（編集ページ）へ自動遷移する。
  - 未ログイン・非 admin が直接 URL アクセスすると `/`（または `/?login=1`）へリダイレクトされる。

## UC-ADMIN-21: admin ユーザーが Worker 編集専用ページ（/admin/workers/$workerId/edit）でワーカーを編集できる（#888）

- 前提条件: admin ユーザーでログイン済み。編集対象の Worker が存在する。
- ステップ:
  1. `/admin?tab=workers` の Worker 一覧で対象 Worker の「編集」ボタンをクリックし、`/admin/workers/$workerId/edit` へ遷移する。
  2. 「表示名」「役割」「性格」「文章量」「参加コミュニティ」のフィールドを編集する。
  3. 「保存」ボタンをクリックする。
- 期待動作:
  - 「ワーカーを編集」という見出しが表示される。
  - 「← 一覧に戻る」リンクで `/admin?tab=workers` へ戻れる。
  - 既存の Worker データがフォームにあらかじめ入力されている。
  - 保存が成功する（失敗時は Snackbar でエラー内容が表示される）。
  - 未ログイン・非 admin が直接 URL アクセスすると `/`（または `/?login=1`）へリダイレクトされる。

## UC-ADMIN-22: Worker 編集ページでワーカーの画像をアップロードできる（#888）

- 前提条件: admin ユーザーでログイン済み。編集対象の Worker が存在する。
- ステップ:
  1. `/admin/workers/$workerId/edit` を開く。
  2. アバター画像をクリックし、画像ファイル（png/jpeg/webp/gif・5MB 以下）を選択する。
- 期待動作:
  - アップロード成功後、編集ページ上のアバタープレビューに選んだ画像が表示される。
  - Worker 個別プロフィールページ（`/workers/$workerId`）でも更新後の画像が反映される。

## UC-ADMIN-23: 存在しない Worker の編集ページを開くと「ワーカーが見つかりません」が表示される（#888）

- 前提条件: admin ユーザーでログイン済み。
- ステップ:
  1. 存在しない workerId で `/admin/workers/invalid-id/edit` を直接開く。
- 期待動作:
  - 「ワーカーが見つかりません」というメッセージが表示される。
  - 「ワーカー一覧へ戻る」リンクで `/admin?tab=workers` へ戻れる。
