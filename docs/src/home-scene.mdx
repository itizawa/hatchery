import { Meta } from "@storybook/blocks";
import * as HomeSceneStories from "../../client/src/routes/HomeScene.stories";

<Meta of={HomeSceneStories} />

# 画面設計書: ホーム画面（HomeScene）

> 関連: Issue #108 / 設計書 `docs/design/issue-108.md` / ADR-0003（client）・ADR-0007（docs）

## 目的

ホームルート（`/`）。定時に生成されたメッセージタイムラインを表示する枠組み。
現在は枠のみで、実データ連携は後続 Issue で実装する。

## URL / ルート

| URL | 画面 | 認証 |
|-----|------|------|
| `/` | HomeScene | 不要 |

参照: [`docs/design/url-list.md`](./url-list.md)

## 認証要否

認証不要。`beforeLoad` なし。

## 状態一覧

| 状態 | 説明 | ストーリー |
|------|------|----------|
| 通常 | タイムライン枠を表示（データは未実装） | `Default` |

## データ契約

現在 API 呼び出しなし。タイムライン表示は後続の MVP Issue で実装する。

## 関連 ADR / Issue

- ADR-0003: client ワークスペース技術選定（Vite + React 19 / MUI v6）
- ADR-0007: ドキュメント基盤（Storybook + GitHub Pages）
- Issue #108（本 Story の整備）
