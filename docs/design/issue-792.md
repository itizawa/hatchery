# 設計書: CLAUDE.md にデザインシステムブロックを追記する（フォント・禁止パターン・参照 UI）(#792)

## 1. 目的 / 背景

現在の CLAUDE.md には UI デザインの方針が記載されておらず、AI が UI を生成する際に Generic な AI スタイル（白紫グラデーション・汎用カードシャドウ等）にデフォルトしやすい。Anthropic 公式の知見によれば、CLAUDE.md にデザイン制約を明示するだけでアウトプット品質が大きく向上する。

## 2. スコープ（やること / やらないこと）

**やること**:
- `CLAUDE.md` に `## デザインシステム` セクションを追加する
- フォント指針（推奨・使用禁止）を記載する
- 禁止パターン（Generic AI スタイル）を列挙する
- 参照 UI（デザイン方向性の基準サービス）を明記する
- カラー規律（`SLACK_COLORS` と矛盾しないアクセントカラー制限）を記載する

**やらないこと**:
- フォントの実際のインストール・適用（別 Issue で対応）
- shadcn/ui への移行
- Anthropic Cookbook aesthetics プロンプト文面の埋め込み（Issue #783 担当）
- コードへの変更

## 3. 受け入れ条件（テストに落とせる粒度で箇条書き）

1. `CLAUDE.md` に `## デザインシステム` セクションが存在する
2. フォント指針として推奨フォント（Plus Jakarta Sans / DM Sans / Geist 等）と使用禁止フォント（Inter・Roboto・Arial のみに頼ること）が記載されている
3. 禁止パターンとして汎用 AI スタイル（白紫グラデーション・汎用カードシャドウ・中央配置 h1+CTA 等）が列挙されている
4. 参照 UI として Reddit 風 AI コミュニティの世界観に合うサービス（Reddit / Linear / Vercel Dashboard 等）が明記されている
5. カラー規律として既存の `SLACK_COLORS` 4 色が基盤として記載され、アクセントカラーの過剰使用を禁止するルールがある
6. 追記内容が既存の `## アーキテクチャ` 等の規約と矛盾しない
7. `pnpm turbo run build lint test` が緑

## 4. 設計方針（アーキ・データ構造・主要モジュール）

- 追記先: `CLAUDE.md` の末尾（既存の `## アイコン規約` セクションの後）
- 書式: 既存の `## バリデーションルール` / `## フォーム規約` セクションと同じ Markdown スタイル
- Issue #783 との分担: 本 Issue は「禁止事項・参照先・フォント・カラー規律」の設計方針ブロック。Anthropic Cookbook の aesthetics プロンプト文面は #783 が担当するため、本セクションでは触れない

## 5. 影響範囲 / 既存への変更（対象ワークスペース: client / server / common / docs）

- `CLAUDE.md`（プロジェクトルート）のみ変更
- client / server / common / docs には一切触れない

## 6. テスト計画（TDD で書くテスト一覧）

本 Issue は Markdown ドキュメントの追記のみであり、コード変更を伴わない。そのため TDD サイクルの「テストコードを書く → 失敗確認 → 実装」は適用外。

確認手順:
- `pnpm turbo run build lint test` を実行し、既存の全テスト・lint が緑であることを確認（CLAUDE.md はコードではないため直接の影響なし）

## 7. リスク・未決事項

- Issue #783（Anthropic Cookbook aesthetics プロンプトの埋め込み）との内容重複が生じないよう、本セクションは「方針・禁止事項・参照先」に留め、プロンプト文面そのものは含めない
