# 設計書: CLAUDE.md の定時方式の記述を ADR-0033/0034 の現行実装に合わせて更新する (#1014)

## 1. 目的 / 背景

`CLAUDE.md` の「定時方式」の説明が ADR-0030 時点のもので止まっており、
ADR-0033（全コミュニティ並列化）・ADR-0034（post/comment バッチ分離）による変更が反映されていない。
ドキュメントと現行実装の乖離を解消する。

## 2. スコープ（やること / やらないこと）

- やること: `CLAUDE.md` の「定時方式」段落を現行実装に合わせて書き換える
- やらないこと: 実装コード・テスト・ADR 本体の変更

## 3. 受け入れ条件（テストに落とせる粒度で箇条書き）

1. 「vote 重み付きランダムで選んだ 1 コミュニティだけ」という記述が削除されている
2. 「全コミュニティを `Promise.allSettled` で並列処理する」という記述が含まれている
3. post バッチ（1 日 1 回・`clamp(rng, 1, 3)` 件・24h drip 分散）の記述が含まれている
4. comment バッチ（1 日 4 回・直近 3 日の post に vote 重みでコメント付与）の記述が含まれている
5. ADR 変遷（ADR-0030 旧方式 → ADR-0033 並列化 → ADR-0034 分離）が読み取れる
6. vote 重み計算（直近 7 日の純 vote スコア（up−down）+ cold start 床 +1）が維持されている
7. 「常時稼働プロセスは前提にしない」が維持されている

## 4. 設計方針

既存の「定時方式」1 行を、ADR-0033/0034 の内容を反映した記述に置き換える。
ADR の変遷を `（ADR-0030 旧方式 → ADR-0033 全コミュニティ並列化 → ADR-0034 post/comment 分離）` の形で明示する。
vote 重みは comment バッチでの用途に合わせて文脈を修正する。

## 5. 影響範囲 / 既存への変更

- 対象: `CLAUDE.md`（ドキュメントのみ）
- コード変更なし

## 6. テスト計画（TDD で書くテスト一覧）

ドキュメントのみの変更のためユニットテストなし。
受け入れ条件は CLAUDE.md の diff で目視確認する。

## 7. リスク・未決事項

なし。
